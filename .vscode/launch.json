{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Start Storybook",
            "program": "${workspaceFolder}/scripts/storybook.js"
        }
    ]
}