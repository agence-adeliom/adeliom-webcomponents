import '../../../dist/awc.js';
import { expect, fixture, html } from '@open-wc/testing';
import type { {{ componentCase tag }} } from "./{{ tagWithoutPrefix tag }}";

describe('<{{ tag }}>', () => {
  it('should render a layout', async () => {
    const el = await fixture<{{ componentCase tag }}>(html` <{{ tag }}></{{ tag }}> `);

    expect(el).to.exist;
  });
});
