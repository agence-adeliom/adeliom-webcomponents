import { getWcStorybookHelpers } from '@awc-storybook/wc-helper';
import { html } from 'lit';
import type { Meta, StoryObj } from '@storybook/web-components';
import type {{ componentCase tag }} from './{{ tagWithoutPrefix tag }}.ts';
const { events, args, argTypes, template } = getWcStorybookHelpers('{{ tag }}');

// More on how to set up stories at: https://storybook.js.org/docs/web-components/writing-stories/introduction
const meta = {
  title: 'Components/{{ tagToTitle tag }}',
  component: '{{ tag }}',
  //tags: ['autodocs'],
  args,
  argTypes,
  parameters: {
    layout: 'padded',
    actions: {
      handles: events
    },
    docs: {
      description: {
        component: ``
      }
    }
  },
  // eslint-disable-next-line @typescript-eslint/no-shadow
  render: context => template(context)
} satisfies Meta<{{ componentCase tag }} & typeof args>;
export default meta;

type Story = StoryObj<{{ componentCase tag }} & typeof args>;

export const Primary: Story = {
  name: 'Default',
  parameters: {
    docs: {
      description: {
        story: ``
      }
    }
  },
  args: {}
};
