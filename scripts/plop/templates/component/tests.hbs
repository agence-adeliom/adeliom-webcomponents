import '../../../dist/awc.js';
import { expect, fixture, html } from '@open-wc/testing';
import type { {{ componentCase tag }} } from "./{{ tagWithoutPrefix tag }}.component.js";

describe('<{{ tag }}>', () => {
  it('should render a component', async () => {
    const el = await fixture<{{ componentCase tag }}>(html` <{{ tag }}></{{ tag }}> `);

    expect(el).to.exist;
  });
});
