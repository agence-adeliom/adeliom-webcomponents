import {{ componentCase tag }} from './{{ tagWithoutPrefix tag }}.component.js';

export * from './{{ tagWithoutPrefix tag }}.component.js';
export default {{ componentCase tag }};

{{ componentCase tag }}.define('{{ tag }}');

declare global {
  interface HTMLElementTagNameMap {
    '{{ tag }}': {{ componentCase tag }};
  }
}
